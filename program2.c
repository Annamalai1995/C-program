#include<stdio.h>
//#include<conio.h>
void main()
{
    char name[20]="";
    char skill[20]="";
    int exp;
    // gets(name);
    // puts(name);
    // gets(skill);
    // puts(skill);
    // gets(exp);
    // puts(exp);
    fgets(skill,sizeof(skill),stdin);
    puts(skill);
    
   
}